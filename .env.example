# Required for the protected Rstream admin panel.
# Do not commit the real password.
ADMIN_PASSWORD=replace-with-a-long-random-password

# Optional: comma-separated frontend origins when frontend/API are on different origins.
RSTREAM_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
PORT=3002
NODE_ENV=production
